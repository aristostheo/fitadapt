# Contributing

Use Python 3.12 and `uv sync`. Before proposing a change run `uv run pytest`, `uv run ruff check .`, and `uv run ruff format --check .`. Keep domain calculations framework-independent, preserve typed immutable contracts, and add focused public-behaviour tests. Never commit personal fitness data; use clearly labelled synthetic data only.
